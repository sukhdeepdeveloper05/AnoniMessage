import React from "react";

const Loading = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="w-16 h-16 border-t-2 border-l-2 border-primary rounded-full animate-spin" />
    </div>
  );
};

export default Loading;
